{
  "name": "@lifecoach/user-state",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo *.tsbuildinfo coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "2.1.5",
    "typescript": "5.7.2",
    "vitest": "2.1.5"
  }
}
