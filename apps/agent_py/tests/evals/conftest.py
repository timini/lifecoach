"""Shared pytest config for evals. Currently no fixtures beyond what
the eval-set JSON files declare; kept as a hook point so future cases
can add per-case stubbing without touching `eval_agent.py`."""

from __future__ import annotations
